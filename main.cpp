/*
** EPITECH PROJECT, 2023
** FraudEmblem
** File description:
** main
*/

#include <iostream>

/*
#include "Context.hpp"
#include "component/Character.hpp"
#include "resource.h"
#include "Dialog.hpp"
*/

int main(void) {
    /*
    auto &win = emblem::Context::window();
    sf::Event e;
    sf::Clock c;
    Dialog d;
   
    d.addEvent(Dialog::DialogEvent::showUI())
        .addEvent(Dialog::DialogEvent::setCharacter(0))
        .addEvent(Dialog::DialogEvent::talk("Hi traveler!"))
        .addEvent(Dialog::DialogEvent::talk("Fuck!"));

    while (win.isOpen()) {


        win.clear();
        while (win.poll(e)) {
            if (e.type == sf::Event::Closed)
                win.close();
        }
        win.display();
        c.restart();
    }
    */
    return 0;
}
